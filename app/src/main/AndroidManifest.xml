<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elif.wep">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/wep_icon_rounded_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/wep_icon_rounded_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".TaskTimer"/>
        <activity android:name=".Schedule" />
        <activity android:name=".Statistics" />
        <activity android:name=".TaskList" />
        <activity android:name=".Profile"  android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar" />
        <activity android:name=".Register" android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"  />
        <activity android:name=".Login" android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>